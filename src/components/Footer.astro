---
import { site, footerNav } from "../lib/site";

const year = new Date().getFullYear();
---

<footer class="border-t border-line bg-bg-elevated">
  <div class="mx-auto grid max-w-6xl gap-10 px-6 py-12 md:grid-cols-12 md:gap-8 md:py-14">
    <div class="md:col-span-3">
      <p class="font-display text-xl tracking-tight text-ink">{site.shortName}.</p>
      <p class="mt-2.5 max-w-xs text-[13px] leading-relaxed text-ink-muted">
        {site.role} · {site.location}. Open to senior frontend / product engineering roles, remote.
      </p>
      <div class="mt-4 inline-flex items-center gap-2 text-[10px] uppercase tracking-[0.18em] text-ink-subtle">
        <span class="size-1.5 rounded-full bg-accent"></span>
        Open to roles
      </div>
    </div>

    {
      Object.entries(footerNav).map(([heading, items]) => (
        <div class="md:col-span-3">
          <p class="text-xs uppercase tracking-widest text-ink-subtle">{heading}</p>
          <ul class="mt-3 space-y-2 text-sm">
            {items.map((it) => {
              const external = it.href.startsWith("http") || it.href.startsWith("mailto:");
              return (
                <li>
                  <a
                    href={it.href}
                    target={external ? "_blank" : undefined}
                    rel={external ? "noopener noreferrer" : undefined}
                    class="text-ink-muted transition-colors hover:text-ink"
                  >
                    {it.label}
                    {external && it.href.startsWith("http") ? " ↗" : ""}
                  </a>
                </li>
              );
            })}
          </ul>
        </div>
      ))
    }
  </div>

  <div
    class="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-3 border-t border-line px-6 py-6 text-xs text-ink-subtle"
  >
    <span>© {year} {site.name}</span>
    <span class="font-display italic">
      Soli Deo gloria.
    </span>
    <span>Built with Astro · Tailwind · Fraunces</span>
  </div>
</footer>
